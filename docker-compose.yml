version: "3.9"

services:
  maville_app:
    image: maville
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maville_app
    environment:
      PORT: 8000
    ports:
      - "8000:8000"


